import { s, v, type LessonVisual } from "./lesson-visual-data.ts";

export const servingVisuals: LessonVisual[] = [
  v("serving-budgets", "timeline", "Latency is a chain of several budgets", "Arrival → queue → prefill → decode", "Quantizing weights does not automatically reduce every latency component.", "A short kernel speedup cannot remove a long queue or a network-bound cache transfer.", s("Queue", "waiting for admission", "Concurrent demand and memory availability determine when a request starts."), s("Prefill", "process prompt positions", "Prompt length and batching shape the time to first token."), s("Decode", "produce successive tokens", "Growing cache traffic and per-step scheduling affect inter-token latency."), s("Response", "stream + finish", "End-to-end latency includes the user-visible delivery path.")),
  v("sampling-contract", "flow", "A decoding policy transforms a vocabulary distribution", "Logits → policy → selected ID", "Greedy choice and random sampling are different contracts.", "Top-k keeps a fixed number of entries; top-p keeps a set whose cumulative probability reaches the threshold under the declared rule.", s("Logits", "scores for V entries", "Start from the model output before sampling transformations."), s("Policy", "temperature + filtering", "Rescale or restrict scores, then normalize over the remaining support."), s("Selection", "argmax or random draw", "Greedy returns the largest score; sampling uses a random draw from the resulting distribution.")),
  v("speculation-economics", "compare", "Accepted progress must repay draft and verification work", "Cost per accepted token", "A high acceptance rate alone does not imply lower latency.", "If a verify round costs twice a normal target step but advances three tokens on average, its target-side cost per token can fall before draft overhead is added.", s("Draft cost", "propose candidates", "The proposer uses compute, state and possibly another device."), s("Verify cost", "evaluate a block", "The target processes candidates together but still performs real work."), s("Progress", "accepted tokens + correction", "Measure useful advance and divide total round cost by that advance.")),
  v("load-test", "timeline", "Load changes the scheduler’s operating point", "Arrival process and service time create a queue", "Closed-loop and open-loop clients impose different load patterns.", "If arrivals exceed sustainable service capacity, the queue grows unless admission or load shedding intervenes.", s("Arrivals", "rate + burstiness", "Record how requests are generated, not just how many clients exist."), s("Scheduler", "batch + token budget", "Batches compete for compute time and cache capacity."), s("Completions", "latency distribution", "Track percentiles and rejected work as well as average throughput.")),
  v("goodput-cost", "compare", "Count requests that meet the service objective", "Useful completions differ from raw completions", "A higher token rate can coexist with worse user-visible service.", "Of 100 completed requests, if only 80 meet the latency objective, the compliant count is 80.", s("Throughput", "all completed work", "Raw output rate ignores whether requests were timely or useful."), s("Goodput", "compliant completed work", "Count results that meet the declared quality and latency objective."), s("Cost", "resources / useful result", "Include idle capacity, rejected work and operational overhead in the stated scope.")),
  v("frontier-map", "hierarchy", "Locate which part of the system a new method changes", "Mechanism before headline result", "Different interventions can compose, but their gains do not automatically multiply.", "A KV compression method changes state; a faster attention kernel changes execution; routing changes which parameters are used.", s("Representation", "weights + state", "Ask which tensors are compressed, quantized or omitted."), s("Architecture", "connectivity + routing", "Ask which computations and dependencies remain."), s("Execution", "kernels + schedule", "Ask where data moves and how operations are mapped to hardware."), s("Service", "placement + queues", "Ask how the method behaves under the target request distribution.")),
  v("frontier-moe", "fork", "A router selects work, then outputs are combined", "Dispatch and combine surround expert computation", "Sparse activation reduces selected compute, not necessarily total parameter storage.", "Top-2 routing sends a token to two selected experts; their outputs return with routing weights for combination.", s("Token + router", "expert scores", "The router computes which expert paths are selected."), s("Expert A", "selected token subset", "An expert processes only the tokens dispatched to it."), s("Expert B", "another selected subset", "Uneven routing can create load imbalance across devices."), s("Combine", "weighted outputs", "Returned outputs must match their original token positions and routing weights.")),
  v("frontier-flash", "timeline", "Overlap movement and arithmetic inside attention tiles", "Tile scheduling preserves the attention contract", "A newer kernel’s speed depends on the device, shapes, precision and implementation.", "Keeping partial softmax state on chip avoids materializing a full score matrix in device memory.", s("Stage operands", "memory → on-chip tile", "Transfer the next query, key or value tile while respecting buffer lifetimes."), s("Compute", "matrix + softmax", "Update scores and stable normalization state for the current tile."), s("Accumulate", "rescaled value sum", "Carry the state needed to combine tiles into the same mathematical attention result.")),
  v("frontier-precision", "memory", "Quantized blocks need both payloads and scales", "A stored code is interpreted through metadata", "Payload bit width alone is not the effective bytes per parameter.", "A block of 32 four-bit values needs 16 payload bytes plus its scale and any alignment overhead.", s("High-precision block", "reference values", "The source values define the error introduced by quantization."), s("Codes + scale", "shared metadata", "Represent values with a limited code range and a block interpretation."), s("Arithmetic", "decode + accumulate", "The kernel’s scaling and accumulation precision determine the resulting error.")),
  v("frontier-test-time", "fork", "Extra inference work creates candidates to evaluate", "Generation budget and selection quality interact", "More candidates help only when generation and evaluation produce useful diversity and discrimination.", "Generating four candidates costs additional tokens and cache work; choosing the longest one is not a correctness guarantee.", s("Prompt", "task + budget", "Define the allowed token, tool and latency budget."), s("Candidate A", "one reasoning path", "A candidate consumes generation resources and can contain errors."), s("Candidate B", "another path", "A second candidate may provide independent information or repeat the same failure."), s("Evaluate", "select or aggregate", "The verifier or aggregation rule determines whether extra work improves the result.")),
  v("capstone-forward", "compare", "The same model serves three different loops", "Training, validation and generation share a forward contract", "Evaluation behavior must disable training-only randomness where required.", "Generation appends predicted tokens; training uses shifted known targets; validation scores held-out examples without updates.", s("Training", "forward + backward", "Compute the loss and update parameters under the training objective."), s("Validation", "held-out forward", "Measure comparable loss without changing parameters."), s("Generation", "autoregressive forward", "Use selected outputs as the next inputs and maintain cache state.")),
  v("capstone-training", "compare", "A training run needs independent checks", "Local arithmetic, learning behavior and restart", "A decreasing loss alone does not prove implementation correctness.", "Overfit a tiny batch, compare a gradient with finite differences, then compare resumed and uninterrupted updates.", s("Gradient check", "local derivative", "Use a small smooth case and account for finite-difference error."), s("Tiny overfit", "can it learn this data?", "A small controlled dataset exposes major objective or update defects."), s("Restart check", "same next update", "Restore optimizer, RNG and data position as well as parameters.")),
  v("capstone-cache", "compare", "Cached and uncached execution should agree", "Same prefix, same next-token scores", "Positions and layer-local state must match the prefix exactly.", "Compare full-prefix logits with token-by-token cached logits before enabling cache-based serving.", s("Full prefix", "recompute all positions", "A simple reference evaluates the complete causal input."), s("Cached path", "append one position", "Reuse old K/V and compute only the new query and state entries."), s("Compare", "logits + tolerance", "Check every step, including multiple sequence lengths and boundary cases.")),
  v("capstone-profile", "compare", "Measure the same model doing the same work", "Equal-work comparisons isolate an implementation change", "Changing prompt length or output count invalidates a simple speedup claim.", "Warm both paths, use identical inputs, and report whether tokenization, transfers and sampling are inside the interval.", s("Baseline", "fixed workload", "Record model state, input shapes and precision."), s("Candidate", "same workload", "Change the intended implementation while retaining equivalent results."), s("Trace", "completed operations", "Explain the observed difference with timing and resource evidence.")),
  v("capstone-http", "cycle", "A request owns resources until completion or cancellation", "Validate → admit → execute → release", "Client disconnects must not leave cache or scheduler slots permanently occupied.", "An aborted response should release request state even if generation has not reached its normal stop token.", s("Validate", "request contract", "Check model options, input limits and error responses."), s("Admit", "capacity + queue", "Reserve only the resources the scheduling policy can support."), s("Stream", "tokens + cancellation", "Return output incrementally while monitoring request lifetime."), s("Release", "all owned state", "Clean up on success, error, timeout and client disconnect.")),
  v("capstone-vllm", "hierarchy", "A serving engine sits between HTTP and model kernels", "API, scheduler, cache and executor", "A command that starts successfully is not a verified service envelope.", "Measure latency and memory across prompt lengths and concurrency after confirming output correctness.", s("API", "requests + tokenization", "The frontend turns external requests into model work."), s("Scheduler", "active token budget", "Admission and iteration policy balance new and ongoing work."), s("Cache manager", "logical → physical", "The allocator maintains request ownership of KV storage."), s("Executor", "shards + kernels", "Devices execute the scheduled model operations and return scores.")),
  v("capstone-assessment", "hierarchy", "A complete result connects claims to artifacts", "Contract → evidence → limits", "Distinguish local CPU, GPU and distributed results explicitly.", "A CPU correctness test and a GPU timing trace support different claims; retain both with their configuration.", s("Contract", "what was implemented", "State supported inputs, precision and behavior."), s("Evidence", "tests + measurements", "Attach commands, logs, outputs and comparable reference results."), s("Limits", "what remains unverified", "Name unavailable hardware, missing workloads and known failure cases.")),
  v("project-trainer", "cycle", "Restart must reproduce the next update", "Checkpoint all state that affects the trajectory", "A checkpoint write must be complete and internally consistent.", "Compare N uninterrupted steps with K steps, save/load, then N−K steps under the same deterministic setup.", s("Train", "parameters + optimizer", "Advance the model and stateful update rule together."), s("Save", "step + RNG + data", "Include the state needed to identify the next batch and stochastic operations."), s("Restore", "rebuild ownership", "Load each state object onto the expected process and device."), s("Compare", "next loss + update", "Check values, not merely whether training resumes without an exception.")),
  v("project-parallel", "compare", "A distributed update should match its reference objective", "Partition work, preserve the global result", "Unequal local token counts require a consistent global normalization.", "Compare one distributed update against a single-process update on the concatenated eligible examples.", s("Reference", "one global batch", "Compute the target gradient under a transparent objective."), s("Partitioned", "rank-local work", "Record which examples, weights and optimizer state each rank owns."), s("Reconciled", "collective result", "Verify the update and account for communication and numerical ordering.")),
  v("project-kernel", "cycle", "Build a correctness envelope around the fast path", "Domain → reference → implementation → evidence", "Unsupported shapes should fail clearly or use a correct fallback.", "Include contiguous, strided, tiny and ragged cases in the declared test domain.", s("Domain", "inputs + precision", "Specify shapes, strides, dtypes and exceptional values."), s("Reference", "simple arithmetic", "Keep an independent implementation available for comparison."), s("Kernel", "layout + synchronization", "Optimize within the declared memory and numerical contract."), s("Evidence", "errors + timing", "Report both the accuracy envelope and completed-work measurements.")),
  v("project-preference", "flow", "An auditable preference update connects data to policy", "Examples → objective → update → evaluation", "Improvement on a reward score is not automatically improvement on every user task.", "Retain the reference policy and exact prompt/response masks when reproducing a preference loss.", s("Preference data", "paired outcomes", "Record how comparisons were collected and filtered."), s("Objective", "declared likelihood rule", "Make policy and reference probabilities, masks and normalization explicit."), s("Evaluation", "held-out behavior", "Look for regressions and distinguish metric movement from broader claims.")),
  v("project-serving", "compare", "Plot an operating envelope instead of one peak", "Load, length and latency form the workload space", "The service objective determines which operating points are acceptable.", "A system may meet its latency target at concurrency 8 and miss it at 32 despite higher raw throughput.", s("Workload grid", "length × concurrency", "Exercise realistic prompt/output mixtures and burst patterns."), s("Service metrics", "TTFT + TPOT + errors", "Record distributions and failures at each operating point."), s("Envelope", "compliant region", "Identify sustainable useful throughput and the cost of capacity.")),
  v("engineering-report", "flow", "Make every result traceable", "Question → method → evidence → interpretation", "A reader should be able to reproduce the measurement or identify what is missing.", "Keep an exact command, environment, raw result and scope statement beside each performance comparison.", s("Question", "falsifiable hypothesis", "State the behavior or bottleneck being tested."), s("Method", "controlled comparison", "Describe inputs, implementation, hardware and timing boundaries."), s("Evidence", "artifacts + uncertainty", "Include observed values, failures and variation."), s("Conclusion", "bounded claim", "Explain what the evidence supports and where it does not apply.")),
  v("c-compile-run", "flow", "Source code passes through several representations", "Translate, link, load, execute", "A successful compilation does not prove memory safety or numerical correctness.", "A missing symbol is a link problem; an out-of-bounds access occurs during execution and needs runtime evidence.", s("Source", "C translation unit", "The compiler checks language rules and emits lower-level code."), s("Object + link", "symbols + libraries", "The linker resolves references into an executable."), s("Execution", "process + memory", "The operating system loads the program and the CPU executes its instructions.")),
];
