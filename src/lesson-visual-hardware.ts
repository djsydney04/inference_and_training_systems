import { s, v, type LessonVisual } from "./lesson-visual-data.ts";

export const hardwareVisuals: LessonVisual[] = [
  v("digital-abstraction", "hierarchy", "Bits are an abstraction over voltages", "Physical behavior supports logical behavior", "Logic levels represent voltage ranges, not exact zero-volt and one-volt values.", "A receiver needs timing and noise margin to decide which logic state a signal represents.", s("Transistor", "controlled conduction", "Devices switch current according to terminal voltages."), s("Gate", "Boolean function", "A network of devices implements a logical relationship."), s("Wire + load", "delay + capacitance", "Signals require time and energy to reach a receiving gate."), s("Bit", "digital interpretation", "Valid voltage ranges let circuits reason in discrete states.")),
  v("binary-and-signed", "memory", "The same bits admit different number systems", "Bit positions carry weighted contributions", "The representation determines the meaning of the highest bit.", "The four-bit pattern 1111 represents unsigned 15 or two’s-complement −1.", s("Unsigned", "weights 1, 2, 4, …", "All bit contributions are nonnegative."), s("Two’s complement", "negative top-bit weight", "The highest bit contributes −2ⁿ⁻¹ in an n-bit signed word."), s("Extension", "preserve the value", "Zero-extend unsigned values; sign-extend two’s-complement values.")),
  v("verilog-combinational", "flow", "Describe a circuit, not a sequence of CPU statements", "Inputs determine outputs through combinational logic", "Assign every output on every path when combinational behavior is intended.", "A missing assignment in a branch can infer retained state instead of the intended pure logic.", s("Inputs", "signals at this instant", "Hardware signals may change concurrently."), s("Expression", "operators + selection", "Synthesis maps arithmetic and conditional expressions into logic."), s("Outputs", "complete assignments", "A combinational output depends only on current inputs and propagation delay.")),
  v("registers-and-fsm", "cycle", "State changes only at its update event", "Current state → next-state logic → register", "Nonblocking assignments model simultaneous registered updates.", "Two registers assigned from one another on a clock edge exchange their old values.", s("State register", "current state", "Registers retain values between active clock edges."), s("Next-state logic", "state + inputs", "Combinational logic computes what should be captured next."), s("Clock edge", "capture next state", "All participating registers sample according to their clocking contract.")),
  v("metastability-cdc-reset", "timeline", "Crossing a clock boundary needs a protocol", "Source clock and destination clock are unrelated", "A synchronizer reduces metastability propagation risk; it cannot make it impossible.", "Synchronize a single-bit control with an appropriate register chain; use a handshake or asynchronous FIFO for coherent multi-bit data.", s("Source", "its own clock domain", "The transition may occur near the receiving edge."), s("Synchronizer", "time to resolve", "A first receiving register may enter metastability before settling."), s("Protocol", "preserve meaning", "Encoding, handshakes and reset release must maintain coherent state.")),
  v("fpga-fabric-resources", "compare", "An FPGA has specialized resources and programmable routes", "Map operations to the right resource", "A multiplication operator does not guarantee a particular DSP mapping.", "A small lookup function may map into a LUT; a multiply-accumulate may use a DSP block; an array may infer block RAM.", s("Logic", "LUTs + registers", "Implements Boolean functions and local state."), s("Arithmetic", "DSP blocks", "Dedicated arithmetic can improve density and speed when inference rules match."), s("Memory", "block RAM + ports", "Storage capacity and port counts constrain simultaneous accesses."), s("Routing", "programmable connections", "Wire delay and congestion can dominate timing after placement.")),
  v("rtl-simulation-synthesis", "flow", "Each tool answers a different question", "Behavior → circuit → placed implementation", "Simulation success does not prove timing closure or a working board.", "An RTL testbench can pass while a placed design fails its clock constraint.", s("Simulate", "does behavior match?", "Exercise signals against a reference and assertions."), s("Synthesize", "which hardware is inferred?", "Inspect logic, memories, arithmetic and unintended latches."), s("Place + route", "can it meet timing?", "Physical placement and routing establish delays and resource constraints.")),
  v("rtl-self-checking", "compare", "A scoreboard connects transactions to expected results", "Drive, observe and compare at accepted transfers", "Latency and stalls must not change which output belongs to which input.", "Queue expected results when valid and ready are both high; compare them in order when outputs are accepted.", s("Stimulus", "legal + edge cases", "Include reset, backpressure and boundary numeric values."), s("Reference queue", "expected transactions", "Track accepted inputs rather than every attempted transfer."), s("Monitor", "accepted outputs", "Check data, ordering and conservation of transactions.")),
  v("pipelined-mac-rtl", "timeline", "Two register stages preserve each transaction", "Product and matching addend → registered sum → consumer", "Ready/valid backpressure may stretch latency; the two-stage datapath is not three registers.", "Without stalls, a captured transaction reaches the sum register on the next edge; the pipeline can accept one transaction per edge after filling.", s("Product register", "stage 1: a×b and c", "Capture the product and its matching addend together when the stage is enabled."), s("Sum/output register", "stage 2: product+c", "The second register holds the complete output transaction until the consumer accepts it."), s("Consumer", "accept when valid and ready", "The receiver handshakes with the output; this box is not a third register in elastic_mac.")),
  v("fpga-memory-banking", "memory", "Ports limit how many operands arrive per cycle", "Addresses choose both bank and word", "More arithmetic units cannot consume unavailable memory ports.", "With bank = word address mod 4, addresses 0, 1, 2, 3 use four banks; 0, 4, 8, 12 collide in one bank.", s("Addresses", "parallel requests", "The access pattern determines whether banks are independent."), s("Banks", "finite ports", "Each bank can service only the accesses its port structure supports."), s("Operands", "delivered this cycle", "Conflicts require arbitration, replication or a different layout.")),
  v("accelerator-dataflows", "flow", "Choose which operand stays local", "Reuse trades storage against movement", "A dataflow is a mapping of loops and values onto hardware.", "Output-stationary execution holds partial sums nearby while input and weight operands move through.", s("Loop nest", "i, j, k", "Matrix multiplication offers reuse along several iteration axes."), s("Stationary value", "weight, input or output", "Keeping one operand local reduces its repeated transfers."), s("Movement", "other operands travel", "The remaining data must arrive at the required rate."), s("Result", "complete reduction", "Partial sums must include each required contribution exactly once.")),
  v("asic-physical-design", "flow", "A logical netlist becomes geometry", "Placement and wires determine the physical cost", "Gate count alone does not determine area, power or clock frequency.", "A long congested route can force buffers and extra delay even when its logical function is simple.", s("Netlist", "cells + connections", "Synthesis chooses logic structures under constraints."), s("Floorplan + placement", "where cells live", "Macro placement, power delivery and congestion shape the layout."), s("Clock + route", "physical wires", "Clock distribution and signal routing introduce real parasitics."), s("Signoff", "timing + power + rules", "Check corners, extraction and physical rules before fabrication.")),
  v("hardware-examples-and-interfaces", "hierarchy", "Interfaces connect independent machines", "Application semantics sit above transport", "Electrical, timing and transaction contracts must all agree.", "A valid payload cannot be transferred if either endpoint disagrees about handshake or reset behavior.", s("Host", "commands + buffers", "Software configures work and owns its memory contract."), s("Interface", "transactions + ordering", "A bridge transfers commands and data with a defined protocol."), s("Accelerator", "compute + local state", "The device reports completion only after the required work becomes observable.")),
  v("fpga-asic-capstone", "cycle", "Prove the datapath before claiming performance", "Reference → RTL → implementation → measurement", "Report separately what was simulated, synthesized and measured on hardware.", "A simulator cycle count is an execution model; it becomes time only after a defensible clock assumption or measurement.", s("Specify", "numeric + interface contract", "Define accepted inputs, output precision, reset and stalls."), s("Verify", "self-checking tests", "Compare against a software reference under adversarial control patterns."), s("Implement", "resource + timing report", "Inspect inferred hardware and the actual critical path."), s("Measure", "equal work", "Account for transfers, host overhead and the steady-state datapath.")),
  v("roofline-contract", "compare", "Two ceilings constrain useful throughput", "min(compute peak, bandwidth × intensity)", "The roofline is an upper bound, not a latency prediction.", "At 2 operations per byte and 100 bytes per time unit, the bandwidth ceiling is 200 operations per time unit.", s("Compute ceiling", "operations / time", "Precision and operation type determine the relevant arithmetic ceiling."), s("Memory ceiling", "bytes / time × ops / byte", "Use bytes transferred at the memory level being analyzed."), s("Attainable rate", "below both ceilings", "Dependencies, launch overhead and utilization can lower realized performance further.")),
  v("async-execution", "timeline", "Overlap requires independent work and resources", "Host enqueue and device completion are different events", "An asynchronous call does not make its result immediately safe to read.", "A host can enqueue a copy and a kernel, but a true dependency still requires the kernel to wait for that copy.", s("Host", "enqueue work", "Submission returns before device work necessarily completes."), s("Device queues", "copies + kernels", "Different engines may overlap when dependencies and resources allow."), s("Completion", "events + synchronization", "A consumer must wait for the event that establishes data readiness.")),
  v("cuda-first-launch", "hierarchy", "A kernel launch creates logical work", "Grid → blocks → threads", "A logical thread index is not a promise about physical execution order.", "For N = 1000 and block size 256, launch four blocks and guard indices at or above N.", s("Grid", "all blocks", "The launch covers the problem domain, often with a ragged final block."), s("Block", "cooperating threads", "Threads in one block can share memory and participate in block barriers."), s("Thread", "one logical index", "Compute a global index and check bounds before accessing memory.")),
  v("cuda-synchronization", "timeline", "Publish shared data before consuming it", "Write → barrier → read", "A block barrier must be reached by every participating thread along a valid path.", "If one warp reads shared memory while another is still writing it, execution timing can change the answer.", s("Producers", "write shared tile", "Each thread stages its assigned elements and handles out-of-range inputs."), s("Barrier", "establish readiness", "The synchronization contract makes prior shared writes available to consumers."), s("Consumers", "use the tile", "Protect the tile from overwrite until all required reads finish.")),
  v("cuda-stable-softmax", "flow", "Reduce before you normalize", "Maximum → exponentials → sum", "Every row uses its own maximum and denominator.", "For logits [1000, 1000], subtracting the maximum gives [0, 0], so probabilities are [0.5, 0.5].", s("Row maximum", "m = max(x)", "A stable reduction finds a common offset."), s("Exponentials", "exp(x − m)", "Subtracting m leaves the exact mathematical softmax unchanged."), s("Normalization", "divide by row sum", "Backward also needs a row reduction over upstream gradient times probability.")),
  v("cuda-complete-harness", "cycle", "The harness is part of the kernel result", "Generate → compare → sanitize → time", "Timing an incorrect or asynchronous unfinished kernel gives a misleading result.", "Use warmup and device events for device timing; keep transfer time separate when reporting a kernel-only measurement.", s("Inputs", "shapes + edge cases", "Exercise strides, small sizes and non-divisible dimensions."), s("Correctness", "reference + tolerance", "Check both expected numerical error and exceptional values."), s("Sanitizers", "memory + race checks", "Look for invalid accesses and synchronization defects."), s("Timing", "completed equal work", "State the device, precision, warmup and included overheads.")),
  v("cuda-tensor-cores", "matrix", "An instruction consumes operand fragments", "Fragments represent pieces of a matrix tile", "The logical matrix shape is different from per-thread register ownership.", "A warp-level matrix instruction distributes fragment elements across participating lanes according to its instruction contract.", s("Memory tile", "layout + alignment", "Load operands in a layout suitable for the instruction path."), s("Fragments", "WMMA lane-owned registers", "This is the warp-level WMMA register interface. The compiler or instruction defines ownership; Blackwell tcgen05 uses a different Tensor Memory path."), s("Accumulators", "partial output tile", "Accumulation precision and later stores determine the final numeric result.")),
  v("cuda-triton-compiler", "flow", "A tensor program becomes machine instructions", "High-level indexing → layout → scheduled code", "A concise program is not evidence of an efficient generated kernel.", "Inspect generated code and profiler evidence when a layout or tile-size change unexpectedly slows the program.", s("Program", "blocked tensor operations", "The source expresses index ranges, masks and arithmetic."), s("Compiler", "layout + lowering", "Passes choose memory access, instructions and resource use."), s("Device code", "registers + instructions", "The final schedule must satisfy hardware dependencies and resource limits.")),
  v("cuda-profile-iterate", "cycle", "Use a hypothesis to choose the next kernel change", "Measure → explain → change → recheck", "Recheck correctness after every optimization that changes execution or arithmetic order.", "If traffic dominates, test operand reuse; if occupancy falls after tiling, inspect register and shared-memory use.", s("Measure", "trace + counters", "Record representative sizes and separate launch overhead from device time."), s("Hypothesis", "one limiting resource", "Relate the observation to a concrete dependency or resource."), s("Change", "one intervention", "Adjust tile size, layout or fusion while preserving the contract."), s("Recheck", "correctness + performance", "Compare equal work and retain regressions as evidence.")),
  v("network-budget", "flow", "A transfer includes more than payload serialization", "Endpoint → link → endpoint", "Keep bytes, bits, directions and aggregates explicit.", "An ideal 400 Gb/s link carries at most 50 GB/s before encoding and protocol overhead; a 1 GB payload needs at least 20 ms of serialization.", s("Sender", "prepare + enqueue", "Memory reads, DMA setup and packetization can add cost."), s("Link", "latency + serialization", "Bandwidth limits payload progress; distance and switches add latency."), s("Receiver", "delivery + consumption", "Data must become available to the consuming operation, not merely arrive at a port.")),
  v("collective-contracts", "compare", "A collective defines ownership of the result", "The same input shards support different outputs", "Every rank must agree on membership, order, shape and operation.", "For inputs [1, 2] and [3, 4], sum all-reduce returns [4, 6] to both ranks; reduce-scatter splits that result.", s("All-reduce", "complete result everywhere", "Each rank receives the reduced tensor."), s("Reduce-scatter", "reduced shards", "Each rank receives only its assigned portion of the reduction."), s("All-gather", "assemble shards", "Ranks collect disjoint inputs into a complete ordered tensor.")),
  v("collective-topology", "hierarchy", "Logical ranks run on physical links", "Communication groups cross different boundaries", "A rank-numbering scheme does not guarantee locality.", "Keep a frequent tensor-parallel group within a fast local domain when the model and system constraints allow it.", s("Device", "local memory", "Kernels produce communication buffers on a specific accelerator."), s("Scale-up", "within the local domain", "Fast device links handle tightly coupled communication."), s("Scale-out", "network between domains", "Adapters and network switches introduce additional latency and contention."), s("Collective", "algorithm + placement", "The schedule must match the actual links, not an imagined uniform network.")),
  v("accelerator-comparison-contract", "compare", "Hold the workload constant when comparing devices", "Same task, different implementation", "Peak numbers for different precision or sparsity assumptions are not directly comparable.", "Compare equal-quality outputs at the same batch, context and latency target before interpreting tokens per second.", s("Workload", "model + shapes + quality", "Declare what must be computed and which errors are acceptable."), s("Implementation", "kernels + runtime", "Compiler, libraries and quantization change realized behavior."), s("Measurement", "latency + cost + energy", "State included overhead, utilization and the service objective.")),
  v("accelerator-memory-atlas", "hierarchy", "Capacity and bandwidth are separate limits", "Near storage is smaller; distant storage has transfer cost", "A model that fits can still be bandwidth-limited.", "Weight bytes are parameter count times stored bytes per parameter, before quantization metadata and runtime allocations.", s("Registers / SRAM", "near execution", "Fast local storage enables reuse but has limited capacity."), s("Device memory", "model + cache", "Larger storage serves repeated parameter and activation traffic."), s("Host / remote", "extra capacity", "Moving state across an interface adds transfer and synchronization costs.")),
  v("accelerator-sram-machines", "memory", "Distributing SRAM changes placement, not total obligations", "Weights and state must live somewhere", "On-chip storage capacity and inter-chip traffic must be counted together.", "If a model spans chips, a layer boundary may require communication even when every local operand is in SRAM.", s("Local SRAM", "explicitly placed state", "Storage sits close to compute units and is often scheduled explicitly."), s("Compute slices", "consume scheduled operands", "The program must deliver data at the right time and place."), s("Inter-chip links", "distributed model state", "Communication carries the values needed by remote computation.")),
  v("accelerator-local-systems", "hierarchy", "A local model shares a finite machine", "Memory, compute and thermal limits interact", "Shared address space does not imply free data movement.", "A desktop model may fit in system memory while its latency is limited by bandwidth or sustained power.", s("Application", "tokenizer + runtime", "CPU work, allocations and request handling consume local resources."), s("Compute", "CPU / GPU / accelerator", "The execution backend determines which kernels and precisions are supported."), s("Memory + power", "shared constraints", "Other applications, bandwidth and thermal limits affect sustainable throughput.")),
  v("accelerator-topology-fit", "compare", "Match the communication pattern to the machine", "Capacity, locality and synchronization shape placement", "Adding devices can increase communication more than it reduces local work.", "A latency-sensitive small batch may run faster with fewer shards if per-layer collectives dominate.", s("Model needs", "weights + cache", "Start with the capacity required for the intended context and concurrency."), s("Parallel method", "what moves each layer", "Choose which tensor or state shards cross boundaries."), s("Machine", "actual link hierarchy", "Place ranks using measured local and network behavior.")),
  v("accelerator-portability-lab", "compare", "Port the contract before tuning the backend", "Reference and target implement the same operation", "Passing one shape does not prove portability across layouts or precision modes.", "Verify a row reduction on contiguous, strided and ragged inputs before comparing backends.", s("Contract", "shapes + numerical rules", "Define supported inputs, reductions and tolerances."), s("Backend", "native programming model", "Map the operation to the target’s execution and memory primitives."), s("Evidence", "tests + measurements", "Keep correctness, compilation and hardware timing as separate claims.")),
  v("profiling-protocol", "cycle", "A profile is an experiment with controlled conditions", "Warm up, capture, interpret, repeat", "A trace records the measured configuration, not a universal property of the model.", "Record sequence length, batch size, precision, device and software versions alongside the trace.", s("Configure", "pin the workload", "Choose representative inputs and declare included overhead."), s("Capture", "steady-state interval", "Avoid mixing initialization with the behavior under study."), s("Interpret", "dependencies + resources", "Distinguish waiting, data movement and useful execution."), s("Repeat", "test the explanation", "Change one condition and check whether the predicted effect appears.")),
  v("kernel-roofline", "matrix", "A tile saves traffic by reusing operands", "Load once, use across several outputs", "Tiling also consumes registers and shared memory.", "A square tile can reuse each loaded row or column value across multiple multiply-adds before evicting it.", s("Load tile", "global → shared", "Cooperatively stage valid inputs and mask the ragged edge."), s("Reuse", "many local products", "Accumulate several output elements from staged operands."), s("Store", "complete outputs", "Write only valid results after the reduction is complete.")),
];
