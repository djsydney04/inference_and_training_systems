import { sourceLink as ref, workedCheck as check } from "./lesson-template";

export const evaluationFoundationsLessons = `
<section class="lesson" id="evaluation-generalization" data-lesson="Generalization and held-out data">
<header><span>Learning beyond the examples already seen</span><h3>A lower training loss answers only the first question</h3></header>
<p>A training run searches for parameters that fit its training objective. The practical question is whether the resulting model performs well on new examples from the intended use. These are different quantities. A model can memorize repeated passages and lower its training loss while failing on unfamiliar passages. This is <strong>overfitting</strong>: the improvement on the fitting data does not carry over as intended.</p>
<p>Separate data by purpose. Training examples supply gradients. Validation examples guide choices such as learning rate, data mixture or which checkpoint to keep. A test set estimates the performance of the selected procedure after those choices are fixed. Repeatedly choosing among experiments based on the test score makes that set part of the selection process, even if its examples never participate in backpropagation.</p>
<div class="derivation"><h4>Read the two loss curves as evidence</h4><p>Imagine a run whose training loss falls from 1.8 to 1.2 to 0.7 nats per token over three checkpoints. Validation loss moves from 1.9 to 1.5 to 1.7 under the same fixed protocol. The middle checkpoint has the best observed validation result. The final checkpoint fits its training data more closely but generalizes worse on that validation distribution.</p><p>Before concluding why, check that evaluation mode, data, context windows, masks and aggregation remained fixed. A changing measurement procedure can imitate an optimization problem. The example describes an observed pattern; it does not by itself identify its cause or establish performance on the final test set.</p></div>
<h4>The unit of the split matters</h4>
<p>Randomly splitting token windows can leak nearly identical text across sets when the windows come from the same document. Group related material before splitting when the intended test is generalization to new documents, repositories, authors or time periods. The split unit should reflect the claim. Holding out a random file is insufficient evidence about a future time period if related future content already occurs in training.</p>
<p>Deduplication reduces some overlap, but a hash match is only one kind of leakage. A benchmark answer may appear in a paraphrase, a solution commentary or an alternative serialization. Conversely, shared common facts do not make every evaluation meaningless. Describe the exclusion rule, known limitations and the population the result is intended to represent.</p>
<h4>A measured loss needs a denominator and a context</h4>
<p>For batches j with negative-log-likelihood sum Sj and valid-target count Nj, corpus token loss is <code>L=(Σⱼ Sj)/(Σⱼ Nj)</code>. It is not generally the arithmetic mean of batch means. If one batch contains 10 targets at mean loss 1 and another contains 90 at mean loss 2, the corpus mean is (10+180)/100=1.9. Averaging 1 and 2 equally would produce 1.5 and over-weight the short batch.</p>
<p>Evaluation also needs a context protocol. Sliding windows, independent chunks, prompt truncation and overlapping target windows can change which context each prediction sees and how often a target is scored. Longer accessible context can reduce loss without changing any weights. Store the tokenizer, checkpoint, context length, stride, valid-target rule and total scored targets with the result.</p>
<p>Regularization and early stopping aim to improve behavior beyond the fitting data. Weight decay changes parameter updates; dropout changes training computation; early stopping selects a checkpoint. They are different interventions. Explain which mechanism is used, keep the evaluation procedure fixed and inspect held-out evidence instead of assuming that any named regularizer guarantees better results.</p>
${check("Why is selecting the lowest test loss from 40 experiments no longer an untouched test of the final choice?", "The test outcomes influenced which experiment was selected. Chance favorable errors can be selected along with real improvements. Treat those data as validation for that search, and evaluate the selected procedure on an appropriately independent test set.")}
${ref("https://d2l.ai/chapter_linear-regression/generalization.html", "Dive into Deep Learning: training error, generalization and data splits")}
${ref("https://d2l.ai/chapter_multilayer-perceptrons/generalization-deep.html", "Dive into Deep Learning: generalization in deep models")}
${ref("https://arxiv.org/abs/2107.06499", "Lee et al.: deduplicating training data and evaluation overlap")}
</section>

<section class="lesson" id="evaluation-uncertainty" data-lesson="Metrics, variation and paired comparisons">
<header><span>How much does a score establish?</span><h3>A small score difference needs an estimate of its variability</h3></header>
<p>Define the outcome before running the model. A task can score exact string match, a normalized answer, a unit-test result or a human judgment under a rubric. Each measures a different property. An exact-match failure caused by extra punctuation is different from a mathematical error; changing the normalization after seeing results changes the evaluation. Keep the prompt template, tools, stop conditions, sampling rule and attempt budget fixed when comparing models.</p>
<p>For n independently sampled binary outcomes yi, where success is 1 and failure is 0, the observed accuracy is <code>p̂=(1/n)Σᵢ yi</code>. Under a common success probability, an estimated standard error is <code>SE≈√(p̂(1−p̂)/n)</code>. This captures sampling variation under those assumptions, not every source of model or dataset uncertainty.</p>
<div class="derivation"><h4>Attach a scale to an observed percentage</h4><p>If 800 of 1,000 independent tasks succeed, p̂=0.8 and SE≈0.01265, or 1.265 percentage points. A rough normal 95% interval is p̂±1.96SE, approximately 77.52% to 82.48%. This approximation is intended for sufficiently large counts away from the boundaries; small samples or nearly all-success outcomes need a more suitable interval.</p><p>The “95%” refers to the long-run coverage of the interval procedure under its assumptions. It does not mean that every future workload has accuracy in that range. Distribution shift, contamination and the choice of benchmark are not included in this sampling-only calculation.</p></div>
<h4>Use the fact that two models answered the same questions</h4>
<p>A paired comparison keeps each task’s two outcomes together. Let di=yB,i−yA,i, so di is −1, 0 or 1. The average difference is <code>Δ̄=(1/n)Σᵢdi</code>. Estimate its standard error as s_d/√n, where s_d is the sample standard deviation of the paired differences. Do not assume the two accuracies are independent when they share the same task set.</p>
<div class="method-table"><table><caption>An illustrative comparison on 100 shared tasks</caption><thead><tr><th>Outcome</th><th>Tasks</th><th>Contribution to B minus A</th></tr></thead><tbody><tr><th>Both succeed</th><td>75</td><td>0</td></tr><tr><th>Only A succeeds</th><td>5</td><td>−5</td></tr><tr><th>Only B succeeds</th><td>7</td><td>+7</td></tr><tr><th>Neither succeeds</th><td>13</td><td>0</td></tr></tbody></table></div>
<p>A scores 80% and B scores 82%, but just 12 disagreements carry the paired difference. Its estimated standard error is about 3.48 percentage points, larger than the observed two-point improvement. This table does not establish a reliable advantage from that difference alone. An interval or resampling analysis should reflect the paired tasks and the chosen inferential assumptions.</p>
<p>A bootstrap estimates sampling variability by resampling observed units with replacement and recomputing a statistic. For paired models, resample task pairs together. For several questions from one document, resample document groups when those groups are the independent units. Resampling individual tokens or splitting a paired record can invent independence that the experiment does not have.</p>
<h4>Different repetitions measure different uncertainty</h4>
<p>Changing a training seed measures variation across trained models. Resampling tasks measures variation due to the evaluated sample. Generating another answer to the same prompt measures the decoding procedure’s variation for that prompt. One kind of repetition cannot stand in for all the others. A deterministic decoder removes that source of sampling variability but leaves task sampling and training variability.</p>
<p>For best-of-k or pass-at-k results, record k and the selection or verifier mechanism. A model given ten attempts has a different budget from a model given one. Report capability together with generated tokens, elapsed time and the relevant resource cost. The <a href="#goodput-cost">serving chapter</a> then connects those outcomes to what a service can sustainably deliver.</p>
${check("A benchmark contains 1,000 questions copied from 20 documents. Why might an interval treating all questions as independent be too narrow?", "Questions within a document can share content, difficulty and failure modes. Positive dependence reduces the effective information relative to 1,000 independent draws. A document-level split and grouped resampling can better match a claim about new documents, although only 20 groups still limits precision.")}
${ref("https://aclanthology.org/P18-1128/", "Dror et al.: statistical significance in NLP evaluation")}
${ref("https://arxiv.org/abs/2103.03098", "Bouthillier et al.: sources of variance in benchmark comparisons")}
</section>

<section class="lesson" id="evaluation-calibration" data-lesson="Calibration and distribution shift">
<header><span>Interpret a probability against an observed event</span><h3>Confidence needs a clearly defined outcome</h3></header>
<p>A probabilistic binary predictor returns q, its estimated probability that an event occurs. It is calibrated when, among comparable predictions assigned probability q, the event occurs at frequency q in the relevant population. If a task classifier assigns 0.8 to 100 suitably sampled cases, roughly 80 successes is consistent with calibration. This is a population relationship, not a promise that any particular set of 100 must contain exactly 80.</p>
<p>A language model’s next-token softmax describes a different event: which token comes next under its learned distribution and context. The model assigning high probability to a fluent answer does not automatically make that probability a calibrated estimate of factual correctness. Sequence likelihood also depends strongly on length. To evaluate answer confidence, define the scored event and how a confidence value is extracted.</p>
<h4>Accuracy and calibration ask different questions</h4>
<p>A binary classifier returning 0.51 and one returning 0.99 can make identical thresholded predictions, while expressing very different uncertainty. Assess calibration with reliability groups and proper scoring rules under a declared protocol. A reliability plot groups similar predicted probabilities and compares their average with observed success frequency; group size and bin choice affect how noisy that comparison appears.</p>
<div class="derivation"><h4>A probability can be penalized without changing the decision</h4><p>For a binary outcome y∈{0,1}, define the Brier score as <code>(q−y)²</code>; lower is better. This is the single-probability binary convention, not the sum over both classes. If success occurs with true probability p=0.75, a prediction q=0.9 has expected score <code>0.75(0.9−1)²+0.25(0.9−0)²=0.21</code>.</p><p>Reporting q=0.75 gives 0.1875. Expand the expected score to obtain <code>E[(q−Y)²]=(q−p)²+p(1−p)</code>. The first term is minimized by q=p; the second is the remaining variation in the outcome. Both q=0.9 and q=0.75 exceed a 0.5 decision threshold, so their accuracy on the same cases is identical despite their different probability quality.</p></div>
<p>Calibration alone is not enough. A classifier that always returns a population’s overall success rate can be calibrated while failing to distinguish easy cases from difficult ones. Inspect whether probabilities separate outcomes as well as whether they match frequencies. Token log loss, task accuracy, confidence quality and service latency describe different aspects of a system.</p>
<h4>A deployment population can differ from the evaluation population</h4>
<p>Distribution shift means the relationship between evaluated data and deployment data has changed. Examples include a new language mix, longer prompts, a new codebase or a change in what counts as a correct answer. A good aggregate score can hide weak performance in a small subgroup, and old calibration can deteriorate when the task population changes.</p>
<p>Write a concise evaluation record: the checkpoint and data revision; the event or task; the input and output protocol; the aggregation rule; the attempt budget; the sample or group count; and the reported variability. Include error examples selected by an explicit procedure. This gives the next experiment a concrete question to answer, and makes the <a href="#pretraining-experiments">ablation plan</a> more useful than a single unlabeled leaderboard number.</p>
${check("A model always predicts a 70% success chance, and succeeds on 70% of cases. Does that establish useful case-by-case confidence?", "No. Its overall rate is consistent with calibration, but its constant prediction cannot identify which cases are likely to fail. Evaluate discrimination and the calibration of meaningful groups, with enough samples to assess their variability.")}
${ref("https://arxiv.org/abs/1706.04599", "Guo et al.: calibration of modern neural networks")}
${ref("https://d2l.ai/chapter_linear-classification/environment-and-distribution-shift.html", "Dive into Deep Learning: environment and distribution shift")}
</section>`;
