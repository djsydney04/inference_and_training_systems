import { v, s } from './lesson-visual-data.ts';
export const cpuVisuals = [
  v('cpu-instructions','flow','From source to architectural state','Compilation and execution preserve a specified behavior','A source statement does not imply one instruction or one cycle.','The RISC-V example explicitly loads x, adds one in a register and stores y.',
    s('Source','variables + control flow','The language defines the program’s permitted behavior; the compiler may transform its implementation.'),s('Instructions','ISA + register names','The binary uses operations and registers described by the instruction set.'),s('Core','decode + schedule + execute','The implementation overlaps work subject to true dependencies and finite resources.'),s('State','registers + memory','Retirement preserves architectural behavior; store visibility also follows the memory-ordering rules.')),
  v('cpu-dependencies','compare','Three events in an instruction’s lifetime','Starting, finishing and committing are different','A completed younger instruction may have to wait before retiring.','An independent add can finish while an older load is waiting, yet still retire after that load.',
    s('Issue','operands and resource ready','Execution begins when its inputs and execution unit are available.'),s('Complete','result produced','A dependent instruction can use the result under this model’s forwarding assumptions.'),s('Retire','oldest completed work','The core commits in program order; precise exceptions can discard younger speculative work.')),
  v('cpu-branches','cycle','A guess becomes evidence for the next guess','Predict, execute, resolve and train','Wrong-path architectural results must not retire.','One loop exit nudges a strongly-taken two-bit counter without immediately reversing its prediction.',
    s('Predict','choose next path','Fetch needs an instruction address before the condition is necessarily available.'),s('Speculate','follow the chosen path','Younger operations can execute before the branch is known.'),s('Resolve','compare with reality','A mismatch redirects the front end and discards wrong-path architectural results.'),s('Train','update predictor state','A later prediction can use information from the resolved outcome.')),
  v('cpu-caches','flow','An address names bytes; the cache finds a line','Line lookup and replacement','A miss at one level does not prove a DRAM access.','Address 260 in the two-way toy cache has line 4, set 0, tag 2 and byte offset 4.',
    s('Address','line + byte offset','The requested bytes belong to an aligned cache line.'),s('Set','candidate ways','The cache’s index selects a bounded group of entries.'),s('Tag check','valid matching line?','Compare tags in the candidate ways; a match supplies the requested offset.'),s('Fill on miss','fetch + replace','This read-only teaching cache fetches one full line and replaces the least recently used entry in its set.')),
  v('cpu-translation','compare','Translation, cached data and fault handling','Three distinct responsibilities','TLB entries are translations, not array values.','With a 4 KiB page, the offset 0x234 survives translation from virtual page 1 to physical frame 0xABC.',
    s('TLB','recent translations','A hit avoids a page-table walk for that translation; permissions still matter.'),s('Data cache','recent memory lines','A hit supplies bytes from a cached line. Some lookup work can overlap address translation.'),s('Page fault','OS intervention','A mapping or permission condition invokes fault handling; it need not involve disk.')),
  v('cpu-load-store','flow','Publish the data before publishing readiness','A one-shot release/acquire protocol','The consumer reads the payload only after acquire observes the release.','The ordinary payload can be 42 because the matching atomic operations establish the required ordering.',
    s('Write data','publisher owns payload','The publisher initializes data that no other thread is reading yet.'),s('Release flag','ready = 1','Release orders the publisher’s earlier accesses before publication.'),s('Acquire flag','observe ready == 1','The consumer must observe the release before using the payload.'),s('Read data','consumer sees payload','The example has no later payload writes. Reusing a buffer requires a new ownership protocol.')),
  v('cpu-simd','flow','Independent elements become vector lanes','A vector loop still has memory and tail work','Grouping floating-point operations must preserve the chosen numerical contract.','Nineteen FP32 elements need two groups of eight plus a three-element remainder for an eight-lane implementation.',
    s('Load','contiguous elements','Addressing and memory traffic still consume resources.'),s('Operate','one instruction, many lanes','Useful lane count depends on vector width, dtype and active masks.'),s('Store','write results','Store bandwidth and aliasing can limit throughput even when arithmetic is efficient.'),s('Tail','remaining elements','A mask or a scalar remainder handles the final partial group.')),
  v('cpu-coherence','cycle','Two private counters can still share one line','Illustrative write ownership, not a named coherence protocol','At least one writer is needed for the harmful sharing pattern.','Eight aligned eight-byte counters fit in one assumed 64-byte cache line.',
    s('Core A writes','owns the line','A’s counter changes, even though B’s neighboring counter does not.'),s('Core B needs it','request write ownership','B must obtain the permissions needed to modify its counter in that same line.'),s('Core B writes','ownership moves','Other copies are coordinated or invalidated according to the protocol.'),s('Core A needs it','request the line again','Repeated handoffs can cost time without any intentional sharing of the counters.')),
  v('cpu-cores-threads','hierarchy','Count the resources behind a thread','Scheduling and physical resources have different boundaries','Two SMT contexts share a core; they are not two full cores.','Nested pools of eight request workers and eight library threads can expose 64 runnable tasks.',
    s('Software thread','OS scheduling unit','A thread can be running, runnable or waiting. Affinity restricts where it may run.'),s('Hardware context','architectural state','SMT keeps several instruction streams resident while sharing execution resources.'),s('Physical core','front end + execution','The core has finite queues, units, bandwidth and power.'),s('Socket / system','caches + controllers + links','Cache-sharing groups and NUMA nodes may not coincide with socket boundaries.')),
  v('cpu-numa','compare','CPU placement and page placement are separate choices','Logical NUMA locality','Pinning an existing thread does not automatically migrate its pages.','Initialize anonymous pages with their intended workers, then verify the actual policy and mapping.',
    s('Local pages','near this worker','A local allocation policy normally starts near the allocating CPU, within allowed resources.'),s('Remote pages','across a node link','The address can remain the same while access crosses an interconnect.'),s('Interleaved pages','spread controllers','A streaming shared workload may value aggregate bandwidth more than one worker’s locality.')),
  v('cpu-measurement','flow','Turn an observation into a tested explanation','A controlled performance experiment','Compare equal useful work and verify the result.','A faster row scan can reflect locality; counters, assembly and controlled sizes help test that explanation.',
    s('Define work','input + output contract','Fix the problem size, numerical semantics and timing boundary.'),s('Measure','elapsed time + counters','Record repetitions, placement and event availability, not only utilization.'),s('Explain','inspect code + bottleneck','Tie a specific hardware or software limit to the observed behavior.'),s('Change one thing','verify + repeat','Retain the original result check and include newly introduced overhead.')),
  v('cpu-practical-lab','compare','The same matrix, two traversal orders','Storage order changes locality without changing the sum','Both traversals must visit every element exactly once.','For row-major double values, rows advance by 8 bytes while columns advance by 8N bytes.',
    s('Row-first','neighboring columns','Use nearby elements within each row before moving to the next row.'),s('Column-first','stride over rows','A fixed column visits a new row at each step, changing cache and TLB behavior.'),s('Verify','same checksum','The companion uses exactly representable values to make equality a clear correctness check.')),
  v('cpu-model-workloads','flow','The host sits on the request’s critical path','Software preparation and model execution','Busy resources do not guarantee useful throughput.','Four exposed milliseconds of CPU work plus six of device work become eight total if the host work is halved.',
    s('Receive','parse + tokenize','Input handling and tokenization consume CPU time and memory bandwidth.'),s('Prepare','schedule + pack','Admission, batching and allocation decide when the next operation can run.'),s('Execute','CPU or accelerator','Numerical kernels use the selected device; CPU submission can remain a dependency.'),s('Return','sample + stream','Output processing and networking matter to observed request latency.')),
];
